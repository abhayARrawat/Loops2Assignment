#include <bits/stdc++.h>
using namespace std;
int main( ) {
int t = 10;
while (t /= 2) {
cout << "Hello" << endl;
}
}
//it will print hello three times bcz  t = 10 / 2 = 5. Since t is non-zero (5) the loop runs, and "Hello" is printed.
// since it iterate 4 times than the value is zero loop is ended.